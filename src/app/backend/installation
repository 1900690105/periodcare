🧩 How to test locally

Put your PDF inside data/docs.pdf.

1.Install dependencies:
pip install fastapi uvicorn chromadb sentence-transformers PyPDF2

2.Install and run Ollama (ollama.com/download), pull a model:
ollama pull phi3:mini

3:upload dataset
python setup_db.py

4.install generative ai
pip install google.generativeai

5.Run:
python -m uvicorn app:app --reload

5.Visit http://localhost:8000/docs → interactive Swagger UI → test /chat.





python installation guide
